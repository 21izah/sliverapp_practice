import 'package:flutter/material.dart';

class MessageDeleteButton extends StatelessWidget {
  const MessageDeleteButton({super.key});

  @override
  Widget build(BuildContext context) {
    return GestureDetector();
  }
}
